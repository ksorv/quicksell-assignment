/**
 *
 * @param {number} value a simple component to show Counter value
 * @returns {jsx} Counter Value
 */
const CounterText = ({ value }) => <p>{`Counter value : ${value}`}</p>

export default CounterText
